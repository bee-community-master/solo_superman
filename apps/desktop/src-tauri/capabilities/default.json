{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the PR-01 scaffold window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
