# Roadmap and Capability Boundaries

Language: [한국어](roadmap_KO.md) | English

The roadmap is a contributor map, not a user-facing UI vocabulary. It mixes code-backed capability bands with preserved backlog boundaries, so a row here is not a claim that the feature is broadly available to end users. Product UI should describe user states such as Spec-ready, Research in progress, Planning-ready, and Waiting for safe execution.

## Current capability map

| Capability band | Contributor meaning | Exit evidence |
| --- | --- | --- |
| Product foundation | Product brief, PRD, UX doctrine, Living Product Spec, Decision Queue. | New contributor can explain the Founder OS loop, and credential-free clarification pipeline and clarification volume smokes prove idea intake -> spec analysis -> active questions -> answer submission -> follow-up/research debt -> Planning Handoff blocker evidence plus 200+ bounded question/answer loops with 100% question-debt completion. |
| Research and evidence | Allowlisted read-only research, bounded batch start for ready public-web tasks, evidence matrix, evidence packs, pro/con/uncertainty, skeptical search. | Research tab evidence matrices and evidence packs with pro/con/uncertainty, source reliability, quality gates, residual risks, next validation actions, and skeptical-search gaps visible; Research review cards with retained source traces, Research-updated Queue with residual risks, source-traceable research-generated follow-up questions explicitly listed instead of hidden, progress metrics that separate active, upcoming, blocked, total follow-up, and open follow-up debt, and a credential-free research pipeline smoke from read-only research run through Decision Queue follow-up debt. |
| Planning handoff | Build Slice Plan, Serve Checklist, Learning Loop Hook, blocker report when not ready. | Confidence Map next-best actions, if-stop-now risk/action artifact, first-class Founder Brief risk/action lists, plus a Planning-ready artifact or explicit blocker artifact. |
| Controlled execution | ExecutionAuthorityRecord ledger plus `file_diff`, `shell_command`, `browser_action` adapters. | Approved and blocked dry-runs with evidence and rollback refs. |
| Full-vision backlog | business/personal mode, business critic intensity, ChatGPT local browser delegation, service page-use permission, implementation step ledger, workspace auto implementation run bootstrap. | Contributors can implement tracked work without inventing new safety defaults; external AI workspace and service-page handoffs show disclosure previews, verdicts, approvals, authorities, browser evidence refs, artifact deletion, revocation, and import/final-submit gates; auto worker controls show Codex runtime readiness, checked-at evidence, adapter/schema/transport evidence, execution mode, account, live-turn state, manual handoff state, and manual ledger-import fallback before local worker execution, the Implementation runtime panel can refresh that readiness in place, and maintainers have opt-in runtime verification commands whose evidence separates skipped, blocked, and passed states plus fixture smokes that prove the `codex_runtime_preview_effect` executor persists a `preview_ready` runtime artifact, a bounded auto-implementation worker job completes ledger import plus stage advancement, generated PR open/body-update/merge mutation guards run without real GitHub writes, every canonical stage can complete with code-review/clean-code/missing-test audit/test evidence, and preview/worker/PR mutation/review-loop evidence is available through one aggregate pipeline smoke, while credential-free browser delegation and service page-use pipeline smokes prove loopback ChatGPT/service-page evidence, artifact cleanup, revocation, and final-submit blocking without real credentials, and the support diagnostics bundle gives error reports a credential-free local evidence path. |

## Phase history

- Phase 1: closed-loop Spec MVP with Research. Core outputs were Decision Queue, Living Product Spec, Completeness, Founder Brief, and ProductEngine reducer contract.
- Phase 1.5A: Background Research Runtime. It stays allowlisted and read-only.
- Phase 1.5B: Execution-readiness Hints. It is readiness metadata, not execution permission, and cannot execute, run, apply, or perform file/shell/browser work.
- Phase 2: Planning Handoff. It produces a final PlanningHandoffArtifact only when evidence and risk gates pass; otherwise it produces a blocker report.
- Phase 2.5: Browser Automation Preview. It compares research quality and delegation risk; it is no-execution and must not submit, write, deploy, mutate, store credentials, share accounts, or implement DTO/API/storage preflight.
- Phase 3: Controlled Execution. It opens MVP action classes only in the sequence common ledger/authority -> `file_diff` -> `shell_command` -> `browser_action`.
- Post-Phase3: Full-vision backlog. It adds purpose mode, critic intensity, local ChatGPT browser delegation, external service permission, implementation step ledger, workspace auto implementation run bootstrap, and cross-platform install/run verification.

## Auto implementation workspace / 자동 구현 작업공간

When planning is detailed enough, Solo Superman may prepare an independent generated-program repo under `workspace/<project>`. The first mounted surface creates a local git repo, `planning-handoff-implementation-plan.md`, `implementation-tracker.md`, seven markdown fallback issue documents, and `.solo-superman/auto-implementation-run.json`, then commits the Solo Superman-generated bootstrap artifacts as a clean git baseline without staging unrelated operator files. The Planning Handoff plan file carries the source-driven `taskBreakdown`, `prIssuePlan`, and `buildSlicePlan`; the tracker and stage issue docs link back to it while recording the delivery/review/verification gates. Those generated docs include scope-specific review evidence slots plus an `ImplementationStepLedger` evidence template for tracker/step docs, commit or no-code evidence, scoped code-review and clean-code-review streaks, missing-test audit evidence, test evidence, blockers, and evidence refs. As stage ticks, worker jobs, and PR mutations move the run forward, the generated `.solo-superman/auto-implementation-run.json` manifest, `implementation-tracker.md` run-state section, per-stage `implementation-issues/*.md` issue-state sections are rewritten with the latest local run state, including stage evidence refs, stage blocker evidence refs, and latest worker evidence refs, and the authoritative `issueManagement.issueDocs[].status` values and issue status summary are kept aligned with completed or blocked stage/worker state. The web implementation panel surfaces the same summary plus each issue row's status, stage, latest worker, blocker, missing evidence, evidence refs, and next action so operators can see completed, blocked, open, and total issue progress without opening every generated document. If a GitHub remote or login is missing, local work continues with a visible remote warning and connection guide instead of pretending issue/PR automation succeeded.

GitHub issue mutation is contract-gated separately from markdown generation. `githubIssueCreation` can request no mutation, a dry-run readiness check, or an approved issue-creation run; any external write still requires connected remote status, per-action approval evidence, rollback plan, audit refs, and verifier coverage. Approved issue creation is limited to `gh issue create` for the generated markdown issue documents, then persists the created GitHub issue URLs beside the local markdown paths and rewrites generated issue-state markdown plus web issue rows with the local issue to GitHub issue pairing. After GitHub issue URLs are recorded for a generated workspace, later approved issue-creation requests for that workspace must not invoke the adapter again. Blocked remote states keep `githubIssueUrls` empty and preserve local markdown issue paths as the source of truth.

GitHub PR mutation uses the same explicit remote-action boundary. The auto implementation run records `open_pr`, `update_pr_body`, and `merge_pr` attempts as `pullRequestMutations`, including issue links, implementation scope, review streak refs, exact verification commands, known gaps, rollback notes, PR-body evidence, merge-readiness evidence, approval, audit, and verifier refs. Generated PR body markdown also carries explicit local markdown issue/GitHub issue traceability, scope-specific code-review and clean-code review refs, and completed-stage implementation evidence refs, missing-test audit refs, and test evidence refs from the `ImplementationStepLedger`, with explicit empty states before any issue URL or stage evidence is complete. Dry-runs stay read-only. Approved PR creation is blocked until the `initial_pr` stage has completed validated implementation ledger evidence, and it is blocked once any PR URL has already been recorded for the run; later work must update or merge that recorded PR instead of opening a duplicate. Approved body updates and merges require connected remote status, per-action approval, and verifier evidence; merges are additionally blocked until the `final_verify_pr_update` stage has completed ledger evidence and the current PR body evidence is present, and approved merge is blocked once a merge has already been recorded for the run.

Local Codex worker jobs are represented as bounded plans before execution. A worker job selects the current stage's markdown/GitHub issue document, stores the generated workspace path, exact planned `ImplementationStepLedger` tracker/step docs, allowed write scope, required ledger evidence, forbidden external/credential actions, and the linked ready-for-execution file-diff `ExecutionAuthorityRecord` reference scoped to that generated workspace with `no_secret_values`. If that authority boundary is missing or not ready, the job remains visibly blocked instead of starting or silently advancing a stage. The worker run bridge attempts a local Codex app-server turn only inside the generated workspace with workspace-write sandboxing, no approval prompts, and network disabled; runtime unavailability or malformed worker output becomes visible `Local Codex worker execution` evidence instead of a silent failure. The Implementation UI can paste/import a current-stage worker ledger JSON envelope for planned or runtime-blocked jobs. A ledger import bridge first checks that imported transitions use the planned tracker/step docs, records matching local worker output through the existing `RecordImplementationStepLedger` reducer, then can mark a planned or runtime-blocked worker job completed only after a completed `ImplementationStepLedger` step validates. The worker stage-advance bridge derives that validated step from the completed current-stage worker job and calls the same stage completion path, so stage advancement still goes through the existing stage endpoint semantics. Operator controls also select only the latest worker job for `currentStage`, so a completed previous-stage worker cannot re-enable stale run/advance actions after the runner advances. Planning another current-stage worker is available only when no current-stage worker exists or the latest worker is blocked by authority/precondition evidence that the run/import/complete/advance controls cannot recover; usable planned, runtime-blocked, ledger-blocked, or completed workers must be continued instead of duplicated.

The controlled runner advances stages only through 5-minute tick records. Completing a stage requires a completed `ImplementationStepLedger` step with tracker/step docs, commit or no-code evidence, two consecutive no-finding code-review passes for feature and repository scopes, two consecutive clean-code passes for changed-code and repository scopes, zero-gap missing-test audit evidence, passing test evidence, and visible blocker history. Completing `merge_main` additionally requires an applied `merge_pr` mutation record and post-merge verification evidence, so the run cannot finish until the PR merge boundary and main verification both have evidence. Production/external final-submit actions remain blocked without a future explicit contract.

## Tracker history

- #86, #87, and #88 established the web/local migration and Phase 3 controlled execution prerequisite gates.
- #91 became the unified tracker for Phase 3 and Post-Phase3 work.
- #92, #93, #94, #95, #96, and #97 covered Phase 3 ledger, approval/API security, `file_diff`, `shell_command`, `browser_action`, and closeout hardening.
- #99, #100, #101, #102, #103, #104, #105, #106, #108, #110, #112, #114, #116, #118, #120, #122, #124, #126, #136, #138, #140, #142, #144, #146, #148, #150, #152, #154, #156, #158, #160, #162, #164, #166, #168, #170, #172, #174, #176, #178, #180, #182, #184, #186, #188, #190, #192, #194, #196, #198, #200, #202, #204, #206, #208, #210, #214, #216, #218, #220, #222, #224, #226, #228, #230, #232, #234, #236, #238, #240, #242, #244, #246, #248, #250, and #252 exposed stage tick/lifecycle controls, manual worker completion, planned worker ledger-doc binding, exact planned worker ledger docs inside bounded worker plans, manual ledger-import binding to those planned docs, current-stage worker control scoping, operator UI/manual recovery for worker ledger imports, current-stage worker replan guards, duplicate PR-open guards, duplicate PR-merge guards, duplicate GitHub issue-creation guards, stage-completion tick-cadence guards, current-batch drafted answer submission for long clarification sessions, bounded batch start for ready public-web research tasks, answer-submission auto-start for ready public-web research tasks when an approved allowlist is active, Queue/Research projection refresh after research-run ingestion, Research card retained source-trace visibility, Research tab evidence matrix and evidence pack validation visibility, Confidence Map next-best action and if-stop-now risk/action visibility, ChatGPT delegation safety verdict visibility, auto worker runtime readiness visibility, Implementation-tab runtime status refresh, runtime checked-at evidence, runtime adapter/schema/transport evidence, runtime execution/account/live/manual fallback state evidence, long-session active/upcoming/blocked/open follow-up progress visibility, research follow-up source traceability through Planning Handoff build-slice evidence, clean git baseline commits for generated auto implementation workspace artifacts, initial-stage evidence gating before approved generated PR creation, generated PR body implementation/missing-test audit/test evidence sections, scope-specific generated PR body review evidence sections, missing-test audit ledger/stage completion refs, generated PR body local/GitHub issue traceability, scope-specific generated markdown review slots, and synchronized generated run manifests, tracker run-state markdown, issue-state markdown, authoritative issue document statuses, issue status summaries after stage/worker/PR transitions, and UI visibility for those summaries, per-issue status/stage details, issue-specific evidence details, issue-specific next actions, generated issue markdown evidence refs, and local/GitHub issue URL pairing, opt-in live runtime readiness verification, runtime preview turn/effect/artifact smoke evidence, auto-implementation worker job smoke evidence through ledger import and stage advancement, generated PR mutation smoke evidence for open/body-update/merge guards, aggregate auto-implementation pipeline smoke evidence that now includes auto-implementation review-loop smoke evidence with missing-test audit refs, merge_main applied PR merge evidence gating, merge_main worker-advance UI gating, merge_main post-merge verification evidence gating, merge_main worker-advance post-merge verification evidence UI gating, worker completion planned-ledger-doc binding, browser delegation pipeline smoke evidence, service page-use pipeline smoke evidence, long clarification volume smoke evidence for 200+ bounded answers, research and clarification pipeline smoke evidence, plus UI visibility that service-page final submit remains blocked until a production-mutation contract even when confirmation and authority refs are present, plus service-page next-action and status-summary copy that limit approval guidance to fill-draft while keeping final submit production-contract blocked, plus full-suite Vitest file-parallelism guardrails, response-body cleanup, live state-version reads for sidecar-heavy verification fixtures, and credential-free support diagnostics bundles for error-report readiness.
- #98 was the temporary standalone Post-Phase3 tracker and is a closed absorbed reference only.

## Transition rule

A later phase cannot reinterpret an earlier no-execution artifact as execution permission. New capability bands must add explicit DTO/API/storage, approval, rollback, audit, and verifier coverage before claiming executable behavior.
